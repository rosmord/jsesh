package jsesh.render.elements;

/**
 * Possible choices for font body size.
 * 
 * Using SMALL body will try to use signs with larger lines, suited for drawing at small scale.
 * 
 * @author Serge Rosmorduc
 */
public enum HieroglyphBodySize {
    STANDARD,
    SMALL
}
