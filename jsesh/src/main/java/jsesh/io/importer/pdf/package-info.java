/**
 * Import Manuel de Codage data hidden in PDF files.
 * <p> This package is <em>not</em> a generic PDF importer. It only reads PDF files produced by
 * JSesh, and extract the JSesh Manuel de codage content included in those files.
 * 
 * Note that it can do it only on files produced by recent (post 2.10) versions of JSesh.
 */
package jsesh.io.importer.pdf;
