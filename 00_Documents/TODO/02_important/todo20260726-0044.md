# Give MDCEditorKeyManager an explicit attach method

Modify `MDCEditorKeyManager` to make its use more transparent. Add an
`attach` method, instead of doing everything in the constructor.

We don't do it immediately, to avoid having one more refactoring to do. We
will wait until the current refactoring is complete and works.
