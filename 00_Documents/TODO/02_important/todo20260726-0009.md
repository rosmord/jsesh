# Generalise JMDCEditorWorkflow::buildAbsoluteGroup

Consider what to do with `JMDCEditorWorkflow::buildAbsoluteGroup`. Could we
make something more generic, like
`transformSelection(Function<List<TopItem>, List<TopItem>>)`?
