# setHasUnsavedChanges should be computed, not stored

The app framework uses `setHasUnsavedChanges` to change its state each time
a change occurs.

This is a bit annoying: we should have a computed, dynamic method for this,
not a boolean value. It makes keeping track of modifications difficult.
