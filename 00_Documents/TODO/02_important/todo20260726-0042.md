# Group styleRef and fontKit into a single config source

Consider grouping `styleRef` and `fontKit` into a single element (e.g.
`JSeshComponentConfigSource`) to use when creating secondary windows and
dialogs. See what needs to be passed, and when.
