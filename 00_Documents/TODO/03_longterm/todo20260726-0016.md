# Improve the structure of ElementDrawer

- The separation between `ElementDrawer` and `DefaultElementDrawer` doesn't
  serve any useful purpose, and is a poor attribution of responsibilities.
- However, we should separate routing from actual drawing.
- We should do away with the mutable fields passed around during the
  drawing process.
- The actual drawing of each element could be performed by a dedicated,
  focused class.
