# Design the page-layout system

Page layout (text size, page size).

Idea about page layout: find the model elements whose layout needs to be
redone, and stack up the layout orders (or, alternatively, redo the layout
directly).

The same order might perhaps be triggered several times (e.g. for a
deformed or aligned element?).
