# Change the column layout algorithm

Change the **column** layout algorithm. Currently, quadrats are processed
the same way in columns and lines. But in a column, vertical signs tend not
to be scaled.
