# Type-safe view update kinds; make Selection a real type

Use the type system to differentiate between the various updates of the
view (e.g. replace `updateView()` by `updateDocumentView()`,
`updateSelectionView()`, etc.). Make `Selection` a proper type, not a
particular case of `TopItemList`.
