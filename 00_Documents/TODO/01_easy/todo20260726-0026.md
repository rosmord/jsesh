# JMDCEditor: test scaling and style-change repaint

`JMDCEditor`:

- [ ] once everything works, ensure that scaling works;
- [ ] check that when the style is modified, the editor is notified and
      repainted.

The old code did:

~~~java
public void setDrawingSpecifications(
        PaintingSpecifications drawingSpecifications) {
    this.drawingSpecifications = drawingSpecifications;
    drawingSpecifications.setGraphicDeviceScale(scale);
    PageLayout p = drawingSpecifications.getPageLayout();
    p.setPageFormat(new PageFormat());
    drawingSpecifications.setPageLayout(p);
    invalidateView();
}
~~~
