# Rename JSimplePalette and switch to MigLayout

Rename `JSimplePalette` (which is not actually a simple palette), and use
MigLayout for `JSimplePalette` instead of Matisse.

(the latest part has been done).
