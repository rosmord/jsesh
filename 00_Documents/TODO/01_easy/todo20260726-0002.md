# Decide the fate of isCanonicalCode in GardinerCode

Check what to do with `isCanonicalCode` in `GardinerCode`.

More generally, look for every occurrence of

~~~java
String canonicalCode = ManuelDeCodage.getInstance().getCanonicalCode(code).code();
~~~

and decide whether the surrounding code should be changed to use
`CanonicalCode` instead of `String`.
