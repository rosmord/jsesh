# Unify naming of observable objects references

We use at least two styles to name observable references to immutable objects:

- `JSeshStyleReference`
- `DirectoryHolder`, used for the optional user hieroglyphs directory.

This is confusing. We should unify the naming, and `Reference` seems to be the best choice.

We should track other instances of this pattern and unify the naming too.


