# Consider making HieroglyphDrawer a local variable

created
: 20260726

closed
: 20260915

`HieroglyphDrawer` is no longer held as an instance variable of `JMDCEditor`.

Consider if `HieroglyphDrawer` could be moved to local variables instead of
being an instance variable. The "true" instance variable is the
`HieroglyphShapeRepository`.
