# Change app startup

created
: 20260821

closed
: 20260915

`AppStartup`/`JSeshStartup.initLookAndFeel()` already installs the look and feel before any other GUI code runs.

To avoid problems linked with the Look and Feel, we should explicitly introduce in the application startup code a function which installs the look and feel **before** any other GUI code is run.

