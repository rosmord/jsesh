# Merge SimpleHieroglyphDatabase with its interface

created
: 20260726

closed
: 20260727

Merge `SimpleHieroglyphDatabase` and its interface `HieroglyphDatabase`, as
it's the only existing implementation.
