# Manage USE_J in YodChoice

created
: 20260726

closed
: 20260915

- `TransliterationUtilities` now handles `USE_J` explicitly.
- Manage `USE_J` in `YodChoice`.
- create the GUI to use it.

Note: `USE_J` has been renamed to `PLAIN_J`, which seemed more logical.
