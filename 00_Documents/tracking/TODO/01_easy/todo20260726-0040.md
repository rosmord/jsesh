# Fix inconsistent accessor naming

created
: 20260726

Find where "modern" accessors without a `get` prefix were used, and move
back to standard getters for code consistency, except when it's not really
a **property**.
