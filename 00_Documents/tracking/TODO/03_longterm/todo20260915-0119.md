# Hieroglypth stacking issue

created
: 20260915

I guess this is more an addition to MdC in a way, but sometimes it would be nice to have a stacking mode that changed from horizontal in column format (like `ra*1`) into vertical in line format (like `ra:1`).  Definitely not a change to the behaviour of `*` and `:`, so I guess it has to be some other construction .

Source: https://github.com/rosmord/jsesh/issues/70 (issue #70)
