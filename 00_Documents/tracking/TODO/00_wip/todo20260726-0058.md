# Support horizontal/vertical grouping like ":" or "*"

created
: 20260726


`HorizontalGrouper`/`VerticalGrouper` exist and are wired into `JMDCEditorWorkflow`.

Horizontal and vertical grouping: do the same as ":" or "*" addition.

(I'm not sure what this is about, but I noticed GUI problems when editing texts in columns, so I'll double check).
