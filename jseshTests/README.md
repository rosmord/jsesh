# JSesh Tests


Various tests used to check if the JSesh libraries work correctly,
or to demonstrate bugs.

As these are interactive programs, they don't belong in the test folder. But neither should they end up in the library jar files.
